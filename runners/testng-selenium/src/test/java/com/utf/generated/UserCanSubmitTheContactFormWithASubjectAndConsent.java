package com.utf.generated;

import com.utf.locator.SeleniumResolver;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.chrome.ChromeDriver;
import org.openqa.selenium.chrome.ChromeOptions;
import org.openqa.selenium.support.ui.Select;
import org.testng.Assert;
import org.testng.annotations.AfterMethod;
import org.testng.annotations.BeforeMethod;
import org.testng.annotations.Test;

import java.util.Map;

/**
 * Auto-generated by universal-test-framework's TestNG/Selenium adapter.
 * Do not edit by hand -- regenerate from the intermediate test definition
 * instead, or your changes will be lost on the next generation run.
 *
 * Target site is read from the TEST_BASE_URL env var
 * (default: http://localhost:8000).
 */
public class UserCanSubmitTheContactFormWithASubjectAndConsent {
    private WebDriver driver;
    private static final String BASE_URL =
        System.getenv().getOrDefault("TEST_BASE_URL", "http://localhost:8000");

    @BeforeMethod
    public void setUp() {
        ChromeOptions options = new ChromeOptions();
        if (!"1".equals(System.getenv("HEADED"))) {
            options.addArguments("--headless=new");
        }
        driver = new ChromeDriver(options);
    }

    @AfterMethod
    public void tearDown() {
        if (driver != null) driver.quit();
    }

    @Test
    public void runs() {
        driver.get(BASE_URL + "/contact");
        SeleniumResolver.resolve(driver, Map.<String, Object>of("type", "field_description", "value", "full name input field")).sendKeys("Jordan Lee");
        SeleniumResolver.resolve(driver, Map.<String, Object>of("type", "field_description", "value", "message textarea")).sendKeys("I'd like a demo of the enterprise plan.");
        new Select(SeleniumResolver.resolve(driver, Map.<String, Object>of("type", "dropdown_description", "value", "subject dropdown"))).selectByVisibleText("Sales");
        WebElement el = SeleniumResolver.resolve(driver, Map.<String, Object>of("type", "checkbox_description", "value", "I agree to be contacted"));
        if (!el.isSelected()) el.click();
        SeleniumResolver.resolve(driver, Map.<String, Object>of("type", "element_role", "role", "button", "name", "Send Message")).click();
        String actualText = SeleniumResolver.resolve(driver, Map.<String, Object>of("type", "page_text", "value", "confirmation banner", "hint", Map.<String, Object>of("test_id", "confirmation-banner"))).getText();
        Assert.assertTrue(actualText.contains("Thanks - we'll be in touch shortly."), "Expected text " + "Thanks - we'll be in touch shortly." + ", got: " + actualText);
    }
}
